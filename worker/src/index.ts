/**
 * Worker entry point. The claim loop, heartbeat, and both agent runners
 * are built out starting Task 13. This stub exists so the workspace
 * scaffold (Task 2) has a real, typecheckable entry point to build on.
 */
import { APP_NAME } from "@core/domain/status.js";

console.log(`${APP_NAME} worker: scaffold only, not yet implemented.`);
