{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "echo \"worker build: pending Task 16 (Docker image bundler decision)\" && exit 1",
    "lint": "echo \"(no lint config yet for worker)\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.278",
    "@anthropic-ai/sdk": "^0.127.0",
    "@core/lead-agent": "workspace:*",
    "@google/genai": "^2.23.0",
    "apify-client": "^2.25.0",
    "firecrawl": "^4.41.0"
  },
  "devDependencies": {
    "@types/node": "^26.6.2"
  }
}
