{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "echo \"no separate build step - runs via tsx directly in the Dockerfile, see worker/Dockerfile's comment\" && exit 0",
    "lint": "echo \"(no lint config yet for worker)\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.278",
    "@anthropic-ai/sdk": "^0.127.0",
    "@core/lead-agent": "workspace:*",
    "@google/genai": "^2.23.0",
    "apify-client": "^2.25.0",
    "dotenv": "^18.0.1",
    "firecrawl": "^4.41.0",
    "tsx": "^4.23.15",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^26.6.2"
  }
}
